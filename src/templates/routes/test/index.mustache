{{ define "test" }}
<div class="container mx-auto my-12 px-4">
    <h1 class="text-3xl font-bold mb-6">WebAssembly Test Suite</h1>

    <div class="mb-8 overflow-hidden">
        <div class="p-6">
            <p class="text-xl mb-4">This area contains various WASM (WebAssembly) test implementations and examples.</p>
            <p class="mb-4">Use the links below to navigate to specific tests:</p>

            <ul class="flex flex-col space-y-2">
                <li class="py-3 flex flex-col space-y-2">
                    <a
                        href="/test/buttons"
                        class="inline-block px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors"
                    >
                        Test interactive button components with WASM
                    </a>
                    <span class="ml-3 text-gray-700 text-gray-400 font-light">
                        Interactive button text utilizing Go's
                        <code>syscall/js</code>
                        support library.
                    </span>
                </li>
            </ul>
        </div>
    </div>

    <p><span class="text-sm text-gray-500">More tests will be added over time.</span></p>
</div>
{{ end }}